

#include "../includes/cube3d.h"

int    ft_parse_file(char **lines)
{
    int i;

    i = 0;
    printf("%s", lines[0]);
    while(lines[i])
    {
        printf("%s", lines[i]);
        i++;
    }
    return (0);
}