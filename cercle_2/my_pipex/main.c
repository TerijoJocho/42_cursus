#include "pipex.h"

int	main(int argc, char **argv)
{
	t_pipex	data;

	// ft_check_args(argc, argv);
	ft_init_pipex(argv, argc, &data);
	// ft_parse_cmds(&data, argv);
	// ft_parse_args();
	// while (cmds)
	// 	ft_exec();
	// ft_cleanup();
	return (0);
}
